apiVersion: 1
providers:
- name: 'default'
  org_id: 1
  folder: ''
  type: 'file'
  options:
    folder: '/var/lib/grafana/dashboards'
datasources:
  - name: 'Prometheus'
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
